fn double(n: Int) -> Int {
    return n * 2
}

fn main() -> Int {
    <print arg={<hoge n={10} />} />

    return 0
}

/*
fn main() -> Int {
    return <fib n={12} />
}

fn fib(n: Int) -> Int {
    return if n <= 2 {
        1
    } else {
        <fib n={n - 1} /> + <fib n={n - 2}/>
    }
}
*/